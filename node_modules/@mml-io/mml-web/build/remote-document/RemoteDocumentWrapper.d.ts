import { RemoteDocument } from "../elements";
import { GraphicsAdapter } from "../graphics";
import { IMMLScene } from "../scene";
/**
 * The RemoteDocumentWrapper class creates an m-remote-document (RemoteDocument) element and initialises it with the
 * given address. It is expected that the `remoteDocument` element will be added to the DOM by the caller.
 */
export declare class RemoteDocumentWrapper<G extends GraphicsAdapter = GraphicsAdapter> {
    readonly remoteDocument: RemoteDocument<G>;
    constructor(address: string, targetWindow: Window, mmlScene: IMMLScene<G>, handleEvent: (element: HTMLElement, event: CustomEvent) => void);
    setDocumentTime(documentTime: number): void;
    overrideDocumentTime(documentTime: number): void;
}
//# sourceMappingURL=RemoteDocumentWrapper.d.ts.map