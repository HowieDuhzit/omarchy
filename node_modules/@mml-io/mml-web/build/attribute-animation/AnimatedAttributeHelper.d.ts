import { MMLColor } from "../color";
import { AnimationType, AttributeAnimation } from "../elements/AttributeAnimation";
import { AttributeLerp } from "../elements/AttributeLerp";
import { MElement } from "../elements/MElement";
import { GraphicsAdapter } from "../graphics";
type AttributeTuple<T extends AnimationType> = T extends AnimationType.Number ? [
    AnimationType.Number,
    AnimationTypeToValueType<T> | null,
    (newValue: AnimationTypeToValueType<T> | null) => void
] : T extends AnimationType.Degrees ? [
    AnimationType.Degrees,
    AnimationTypeToValueType<T> | null,
    (newValue: AnimationTypeToValueType<T> | null) => void
] : [
    AnimationType.Color,
    AnimationTypeToValueType<T> | null,
    (newValue: AnimationTypeToValueType<T> | null) => void
];
export type AttributeHandlerRecord = Record<string, AttributeTuple<AnimationType.Number> | AttributeTuple<AnimationType.Degrees> | AttributeTuple<AnimationType.Color>>;
type AnimationTypeToValueType<T extends AnimationType> = T extends AnimationType.Number | AnimationType.Degrees ? number : MMLColor;
/**
 * The AnimatedAttributeHelper is a utility class that manages the application of attribute animations to an element.
 *
 * It is used by an MElement that has animateable attributes and is responsible for applying the animations to the
 * element according to the precedence rules defined in the AttributeAnimation<GraphicsAdapter> class, and falling back to the element's
 * attribute value if no animations are active.
 */
export declare class AnimatedAttributeHelper {
    private element;
    private handlers;
    private stateByAttribute;
    private allAnimations;
    private allLerps;
    private documentTimeTickListener;
    private hasTicked;
    constructor(element: MElement<GraphicsAdapter>, handlers: AttributeHandlerRecord);
    addSideEffectChild(child: MElement<GraphicsAdapter>): void;
    removeSideEffectChild(child: MElement<GraphicsAdapter>): void;
    elementSetAttribute(key: string, newValue: AnimationTypeToValueType<AnimationType> | null): void;
    getAttributesForAttributeValue(attr: string): Array<string>;
    addLerp(lerp: AttributeLerp<GraphicsAdapter>, attributeValue: string): void;
    removeLerp(lerp: AttributeLerp<GraphicsAdapter>, attributeValue: string): void;
    addAnimation(animation: AttributeAnimation<GraphicsAdapter>, key: string): void;
    removeAnimation(animation: AttributeAnimation<GraphicsAdapter>, key: string): void;
    updateTime(documentTime: number): void;
    reset(): void;
}
export {};
//# sourceMappingURL=AnimatedAttributeHelper.d.ts.map