import { VideoSource } from "./VideoSource";
export declare class WHEPVideoSource implements VideoSource {
    private srcURL;
    private videoTag;
    private peerConnection;
    private stream;
    constructor(srcURL: URL, videoTag: HTMLVideoElement);
    getContentAddress(): string;
    dispose(): void;
    syncVideoSource(): void;
    static isWHEPURL(url: URL): boolean;
}
//# sourceMappingURL=WHEPVideoSource.d.ts.map