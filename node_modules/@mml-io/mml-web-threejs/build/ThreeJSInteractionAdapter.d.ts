import { Interaction } from "@mml-io/mml-web";
import * as THREE from "three";
import { ThreeJSGraphicsAdapter } from "./ThreeJSGraphicsAdapter";
export declare class ThreeJSInteractionAdapter {
    private static worldPos;
    private static matrix;
    private static frustum;
    private static raycaster;
    private static intersections;
    private static direction;
    static interactionShouldShowDistance(interaction: Interaction<ThreeJSGraphicsAdapter>, camera: THREE.Camera, scene: THREE.Scene): number | null;
    static getRaycastResults(a: THREE.Vector3, b: THREE.Vector3, distance: number, scene: THREE.Scene): THREE.Intersection<THREE.Object3D<THREE.Object3DEventMap>>[];
    static hasAncestor(object: THREE.Object3D, ancestor: THREE.Object3D): boolean;
}
//# sourceMappingURL=ThreeJSInteractionAdapter.d.ts.map