import * as THREE from "three";
/**
 * The ThreeJSClickTrigger class is responsible for handling click events on the MML scene and raycasts into the scene to
 * determine which object was clicked and then dispatches events to those elements.
 */
export declare class ThreeJSClickTrigger {
    private clickTarget;
    private rootContainer;
    private camera;
    private eventHandlerCollection;
    private raycaster;
    private mouseDownTime;
    private mouseMoveDelta;
    static init(clickTarget: Document | HTMLElement, rootContainer: THREE.Object3D, camera: THREE.Camera): ThreeJSClickTrigger;
    private constructor();
    private handleMouseDown;
    private handleMouseUp;
    private handleMouseMove;
    private handleClick;
    dispose(): void;
    private isMaterialIgnored;
}
//# sourceMappingURL=ThreeJSClickTrigger.d.ts.map