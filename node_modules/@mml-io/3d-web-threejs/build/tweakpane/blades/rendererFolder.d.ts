import { EffectPass } from "postprocessing";
import { WebGLRenderer } from "three";
import { FolderApi } from "tweakpane";
export type RendererValues = {
    shadowMap: number;
    toneMapping: number;
    exposure: number;
};
export declare function createDefaultRendererValues(): RendererValues;
export declare class RendererFolder {
    private rendererValues;
    private folder;
    private monitoredValues;
    constructor(parentFolder: FolderApi, rendererValues: RendererValues, expand?: boolean);
    setupChangeEvent(renderer: WebGLRenderer, toneMappingFolder: FolderApi, toneMappingPass: EffectPass | any): void;
}
//# sourceMappingURL=rendererFolder.d.ts.map