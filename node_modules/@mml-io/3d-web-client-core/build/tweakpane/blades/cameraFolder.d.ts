import { FolderApi } from "tweakpane";
import { CameraManager } from "../../camera/CameraManager";
export type CameraValues = {
    initialDistance: number;
    minDistance: number;
    maxDistance: number;
    initialFOV: number;
    maxFOV: number;
    minFOV: number;
    invertFOVMapping: boolean;
    damping: number;
    zoomScale: number;
    zoomDamping: number;
};
export declare function createDefaultCameraValues(): CameraValues;
export declare class CameraFolder {
    private cameraValues;
    folder: FolderApi;
    private camData;
    constructor(parentFolder: FolderApi, cameraValues: CameraValues);
    setupChangeEvent(cameraManager: CameraManager): void;
    update(cameraManager: CameraManager): void;
}
//# sourceMappingURL=cameraFolder.d.ts.map