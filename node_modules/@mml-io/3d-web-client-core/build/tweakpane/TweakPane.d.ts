import { CameraManager } from "../camera/CameraManager";
import { LocalController } from "../character/LocalController";
import { CameraValues } from "./blades/cameraFolder";
import { CharacterControllerValues } from "./blades/characterControlsFolder";
export type TweakPaneConfig = {
    cameraValues: CameraValues;
    characterControllerValues: CharacterControllerValues;
};
export declare class TweakPane {
    guiVisible: boolean;
    private gui;
    private camera;
    private characterControls;
    private saveVisibilityInLocalStorage;
    private tweakPaneWrapper;
    private eventHandlerCollection;
    constructor(holderElement: HTMLElement, config: TweakPaneConfig, guiVisible?: boolean);
    private processKey;
    dispose(): void;
    setupCamPane(cameraManager: CameraManager): void;
    setupCharacterController(localController: LocalController): void;
    updateCameraData(cameraManager: CameraManager): void;
    updateCharacterData(localController: LocalController): void;
    private formatDateForFilename;
    private downloadSettingsAsJSON;
    private importSettingsFromJSON;
    private toggleGUI;
    setVisible(visible: boolean): void;
    private updateVisibility;
}
//# sourceMappingURL=TweakPane.d.ts.map