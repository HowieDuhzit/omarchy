import { AnimationClip, Group, LoadingManager } from "three";
import { KTX2Loader } from "three/examples/jsm/loaders/KTX2Loader.js";
export type ModelLoadResult = {
    group: Group;
    animations: Array<AnimationClip>;
};
export declare class ModelLoader {
    private manager?;
    private options;
    private fbxLoader;
    private gltfLoader;
    private static dracoLoader;
    private static ktx2Loader;
    private static getDracoLoader;
    constructor(manager?: LoadingManager | undefined, options?: {
        requestHeader?: Record<string, string>;
        withCredentials?: boolean;
    });
    private static getKTX2Loader;
    static getSharedKTX2Loader(): KTX2Loader;
    load(url: string, onProgress?: (loaded: number, total: number) => void, abortController?: AbortController): Promise<ModelLoadResult>;
    private static extractUrlBase;
    loadFromBuffer(buffer: ArrayBuffer, pathName: string): Promise<ModelLoadResult>;
}
//# sourceMappingURL=ModelLoader.d.ts.map